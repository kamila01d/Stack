To run this program:
                make run
To clean folder from executables:
                make clean

This program is implementation of ADT stack. It uses template, so the things in stack can be anything you want. The stack is using an array. 
It also has basic methods such as push, pop, size, clear, full, empty. 
